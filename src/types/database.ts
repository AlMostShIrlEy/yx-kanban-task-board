// Placeholder for the Supabase-generated Database type.
// After Phase 1 SQL migration is run, regenerate with:
//   npx supabase gen types typescript --project-id <id> > src/types/database.ts
// Then update src/lib/supabase.ts to use createClient<Database>(url, anonKey).
//
// Note: auth-related types (User, Session, AuthChangeEvent) are NOT row
// types and are NOT generated here. Import them directly from the SDK:
//
//   import type { Session, User } from '@supabase/supabase-js'
export type Database = unknown
